#include <obliv.h>
#include <obliv.oh>

typedef struct
{
	char *src;
	int rows;
	int cols;
	float **mat;
} Io;

typedef struct
{
{{{STRUCTS}}}
    int outRows;
    int outCols;
	char *out;
	float **ret;
} protocolIo;

typedef struct
{
	int rows;
	int cols;
	obliv float **mat;
} intermediateMat;

void protocol(void *args);
