#include <obliv.h>
#include <obliv.oh>

#define ROWS {{{NUM_ROWS}}}
#define COLS {{{NUM_COLS}}}

typedef struct
{
	char *src;
	char *out;
	int rows;
	int cols;
	float mat[ROWS][COLS];
	float **ret;

} protocolIo;

typedef struct
{
	int rows;
	int cols;
	obliv float **mat;

} intermediateMat;

void protocol(void *args);
