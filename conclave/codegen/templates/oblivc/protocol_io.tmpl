#include <obliv.h>
#include <obliv.oh>

typedef struct
{
	char *src;
	char *out;
	int rows;
	int cols;
	float **mat;
	float **ret;

} protocolIo;

typedef struct
{
	int rows;
	int cols;
	obliv float **mat;

} intermediateMat;

void protocol(void *args);
