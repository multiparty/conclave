function project(table, cols){
    var outputTable = []

    for(var i=0; i<table.length; i++){
        var arr = [];
        for(var j=0; j<cols.length; j++{
            arr.push(table[i][j]);
        }
        outputTable.push(arr);
    }

    return outputTable;
}
{{{OP_CODE}}}