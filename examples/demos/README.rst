========
Python Demo
========

A single-party conclave demo which compiles the protocol specified in `demo.py` to Python and executes it. The input
data is stored under `data/input_relation.csv`. The output will be stored under `data/summed.csv`. To run invoke the
following in a terminal: `python3 demo.py`.
