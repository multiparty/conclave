========
Python Demo
========

A single-party conclave demo which compiles the protocol specified in :code:`demo.py` to Python and executes it. The
input data is stored under :code:`data/input_relation.csv`. The output will be stored under :code:`data/summed.csv`.
To run invoke the following in a terminal: :code:`python3 demo.py`.
