from pyspark import SparkContext, SparkConf
import numpy, functools, operator, sys

conf = SparkConf().setAppName("{{{JOB_NAME}}}").setMaster("{{{SPARK_MASTER}}}")
sc = SparkContext(conf=conf)

inpt_idx = 1

{{{OP_CODE}}}
